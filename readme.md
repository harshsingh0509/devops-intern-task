Completed the devops internship assessment
Github repo - 

Workflow
- Dockerized django application with gunicorn by writing the dockerfile.
- created docker-compose file for handiing multiple containers at once
- Used commands such as docker build and docker compose for creating the image and running the container.
- wrote the workflow deploy.yml file for ci/cd implemenation reference and for the deployment.
- explored render for the deployment part by connecting github repo to it and getting deployment url.

